import { DisplayFilterPipe } from './display-filter.pipe';

describe('DisplayFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
